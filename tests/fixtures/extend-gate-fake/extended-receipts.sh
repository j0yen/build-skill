#!/usr/bin/env bash
# Fake extended-receipts.sh for extend-gate.sh integration tests — the 17
# extended producers are irrelevant to the delta-baseline logic under
# test, so this just succeeds without writing anything.
exit 0

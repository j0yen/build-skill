# PRD-fixture-no-deferral (test fixture)

Status: Draft v0.1
build_target: shell
build_priority: low

## TL;DR

Sibling fixture with NO `deferred_acs:` field. Verifies the default
emission is `[]` and the no-deferral path is unchanged.

## Acceptance

1. JSON parse — code-pairable.
2. Trailer unchanged — code-pairable.
